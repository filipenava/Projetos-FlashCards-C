{
  "name": "flashcards-c",
  "version": "1.0.0",
  "description": "Sistema de Flashcards para aprendizado de Linguagem C",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "patch-package",
    "deploy": "npm run build && firebase deploy --only hosting",
    "deploy:all": "npm run build && firebase deploy",
    "firebase:serve": "npm run build && firebase serve"
  },
  "dependencies": {
    "firebase": "^12.5.0",
    "pinia": "^2.2.6",
    "vue": "^3.5.13",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "autoprefixer": "^10.4.20",
    "patch-package": "^8.0.1",
    "postcss": "^8.4.49",
    "postinstall-postinstall": "^2.1.0",
    "tailwindcss": "^3.4.15",
    "vite": "^6.0.3"
  }
}
